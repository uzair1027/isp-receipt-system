{
  "buildCommand": "cd backend && pip install -r requirements.txt",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/backend/api/v1/$1" },
    { "source": "/(.*)", "destination": "/frontend/$1" }
  ]
}
